# 鼓手节拍器 PWA：项目说明（给协作的 AI / 评审者）

这份文件让新加入的助手（Codex 等）不用翻聊天记录，就能看懂项目、跑起来、找到该评审的地方。

## 1. 路径与地址

| 项 | 位置 |
|---|---|
| 项目根目录 | `E:\cloud\drum-metronome-pwa` |
| Git 仓库 | https://github.com/qw493933161-coder/drum-metronome （公开，`main` 分支） |
| 线上地址 | https://qw493933161-coder.github.io/drum-metronome/ （GitHub Pages，推送 `main` 后约 1 分钟生效） |
| 仓库外的练习谱文件 | `E:\cloud\鼓谱-GuitarPro官方20首\`（Guitar Pro 官方免费鼓组 20 首，**有版权，不能进仓库**）、`E:\cloud\鼓谱-练习\`（自制的 MusicXML 练习谱） |

本地预览：`node scripts/serve.js`，然后打开 http://localhost:5173 。没有构建步骤、没有依赖安装，改完刷新即可。

## 2. 用户与目标

- 用户在学架子鼓（在家用哑鼓垫练），**独自对着谱练、追求节拍准**。设备是**两台安卓**（手机 + 平板），用 Chrome 打开并装到主屏幕。
- 需求原则：界面简单（参考市面上的 Metronome Beats），**一次加一个功能**，用户在手机上验收后才推送。
- 界面文字、代码注释都用中文。

## 3. 功能一览（顶部 5 个标签）

1. **节拍器**：每小节拍数 1–16、每拍细分 1–8、每拍可单独设节奏型（用音符图标显示），细分提示音。
2. **基础节奏**：17 个鼓组节奏（摇滚、放克、华尔兹、Shuffle 等），五线谱鼓谱 + 播放光标，可全屏、横屏。
3. **鼓垫练习**：28 个手序练习（R/L、重音 `>`），谱面带手序。
4. **变速练习**：把不同时值拼成一页 4/4 谱；点某一拍替换；数字记谱（`123. 1.34 12.. 1234`）；随机出题；保存 / 另存为 / 导入导出（`DRUM1:` 文本）；每遍之间的预备拍。
5. **曲谱**：用 alphaTab 打开 Guitar Pro（.gp/.gp3–5/.gpx）或 MusicXML，跟随播放、速度、鼓声/伴奏静音、按小节号循环、全屏看谱。谱文件存在 IndexedDB。

全局：开始前倒计时、第一拍重音开关（默认关）、渐进加速、随机静音小节、每个练习记住自己的速度、参考音/鼓声两路音量、声画同步校准、横屏锁定、后台播放保活、云同步（秘密 Gist，扫码配对）、自动更新。

## 4. 文件结构

| 文件 | 行数 | 作用 |
|---|---|---|
| `index.html` | ~320 | 全部界面结构（单页，各标签是 `.view`，用 `hidden` 切换） |
| `css/app.css` | ~370 | 全部样式；深色主题；`(orientation: landscape) and (max-height: 500px)` 是手机横屏布局 |
| `js/metronome.js` | ~2450 | 主程序：状态、音频调度、4 个练习页、谱面 SVG 绘制、设置、各种桥接对象 |
| `js/sync.js` | ~425 | 云同步：合并算法、GitHub Gist 接口、扫码配对 |
| `js/song.js` | ~490 | 曲谱页：alphaTab 封装、IndexedDB 存谱、云端谱库、全屏、按小节循环 |
| `service-worker.js` | ~70 | 离线缓存：页面/脚本走“网络优先”，`vendor/` 走“缓存优先” |
| `vendor/alphatab/` | — | alphaTab 1.8.4（MPL-2.0）、Bravura 字体（OFL）、Sonivox 音色（Apache-2.0） |
| `vendor/qrcode/` | — | qrcode-generator 2.0.4（MIT），扫码配对时生成二维码 |
| `tests/` | — | 自动化测试，见第 7 节 |
| `scripts/serve.js` | 本地静态服务器 | `scripts/gen_icons.js` 生成图标 |

三个 JS 都是立即执行函数（IIFE），用 `window.XxxBridge` 互相通信，加载顺序：`metronome.js` → `sync.js` → `song.js`。

## 5. 关键设计（评审前请先读）

### 5.1 音频调度（`metronome.js`）
- Web Audio 提前排程：`setInterval(tick, 25)`，前瞻 `lookahead()` 前台 0.12 秒、**后台 4 秒**（后台定时器会被系统降速）。
- 四个调度函数：`schedule()`（节拍器）、`scheduleGroove()`、`schedulePad()`、`scheduleCombo()`，都往 `queue` 里放 `{t, ...}` 事件。
- 音色全部合成（振荡器 + 噪声），不加载采样。信号链：音源 → `clickBus`（参考音）/ `drumBus`（鼓声）→ `gate`（随机静音）→ `out` → 扬声器。
- 画面高亮不跟 rAF，而是 `setInterval(drain, 8)` 按音频时钟出队：`now = ctx.currentTime - visualLag()`，`visualLag = 设备报告的输出延迟 + 用户校准 avOffset - SCREEN_LEAD(30ms)`。
- 练习辅助：`onBar(t)` 每小节决定是否静音（用 `gate.gain.setValueAtTime`）；`onPass(t)` 每遍决定是否加速。
- **两个速度**：`s.bpm` 是用户设定、会保存；`audioBpm` 是正在排程的速度（渐进加速只改它），`shownBpm` 是屏幕上显示的速度。渐进加速从不写回 `s.bpm`，停下后自然回到起始速度。
- **改设置在下一拍生效**：每个正拍记一个检查点 `rememberBeat()`；调速、改细分、换节奏型、改谱时调用 `rewindAtBeat()`——撤销下一个还没出声的正拍之后已排好的音源（`sources` 登记了每个音源），把排程状态退回该拍，再按新设置重排。所以后台预排的 4 秒不会用旧速度播完。
- **过期不补播**：音源时间早于 `ctx.currentTime + 5ms` 就不发声，只推进位置，节拍网格不变；落后超过 30 秒（系统长时间冻结）直接停止并提示。
- **启动防重入**：`starting` + `startGeneration`，连点开始、启动中取消都不会留下定时器或音源。倒计时、预备拍也按拍逐个排程（`countLeft`、`gapLeft`），同样能被撤销。
- 后台保活：播放时循环一段静音 `<audio>` + Media Session（锁屏控制）。

### 5.2 变速练习的时间单位
- 1 拍 = 12 个单位（十六分 = 3，八分 = 6，三连音八分 = 4），1 小节 = `BAR` = 48。
- 谱是 figure 数组：`{k:'n', d}` 音符、`{k:'r', d}` 休止、`{k:'c', b:'1011'}` 一拍内的节奏格。`layoutCombo()` 自动分小节，`comboSvg()` 画谱。

### 5.3 状态与保存
- 主状态对象 `s`，存 `localStorage['metronome-v2']`，每次改动调用 `save()`。
- `SYNC_KEYS` 里的项会同步；`save()` 对比快照，给改动的项在 `s.stamps[key]` 记时间戳。
- **只在本机、不同步**：`avOffset`（声画校准）、`volClick/volDrum`（音量）、`hist`（撤销历史）、`mode`（当前标签）、曲谱的放大比例和上次打开的谱。

### 5.4 云同步（`sync.js`）
- 存储：用户自己 GitHub 账号下的**秘密 Gist**。设置和变速谱在 `drum-metronome-sync.json`；曲谱文件在另一个 Gist（描述 `drum-metronome-songs…`），每份谱一个 `s-<id>.json`（base64）。
- 合并 `merge(L, R)`：设置项按时间戳“后改的赢”（`beats/subdiv/cells` 三项必须整组取同一边；`tempos`、`songPref` 按每个练习 / 每一项分别比较时间戳，时间戳记在 `stamps` 的 `@组名/键` 里）；“我的谱”按 `updated` 取新，删除用墓碑 `gone`（保留 90 天）。
- 触发：改动后 2.5 秒、打开 App、回到前台、联网、每 2 分钟；**播放中不同步**。
- 令牌：只存 `localStorage['metronome-sync-v1']`，不进 Gist、不进导出、不打印。
- 扫码配对：已开启的设备显示二维码（`DRUMSYNC1:` + base64 的 `{t, g, s}`），新设备用 `BarcodeDetector` + 摄像头扫码接入；两分钟后自动关闭。

### 5.5 本地存储键
`metronome-v2`（主状态）、`metronome-sync-v1`（令牌与 Gist 编号）、`metronome-song-v1`（曲谱放大比例、上次打开）、`metronome-song-gone`（曲谱删除记录）、IndexedDB `metronome-songs`（谱文件）。

### 5.6 更新机制
- Service Worker 页面/脚本“网络优先”（3.5 秒超时退回缓存），`vendor/` 缓存优先；新版本接管后自动刷新一次（播放中等停下再刷）。
- **改动任何预缓存文件都必须把 `CACHE_NAME` 加一**（当前 `drum-metronome-v48`）。

## 6. 约束与红线

- **版权**：仓库和 Pages 都是公开的。有版权的歌曲谱文件绝不能放进仓库，只能由用户自己导入到本机 / 自己的秘密 Gist。
- **令牌安全**：不要把任何令牌写进代码、日志、URL；不要扩大令牌权限（只用 `gist`）。已知风险：站点和用户另一个项目 `lesas-book` 共用 `qw493933161-coder.github.io` 域名，理论上同域脚本能读到 localStorage 里的令牌。
- **不要随手推送**：用户要先在手机上验收。提交作者用 `qw493933161-coder <245834427+qw493933161-coder@users.noreply.github.com>`（本机没有全局 git 身份，用 `git -c user.name=… -c user.email=… commit`）。
- 不要用浏览器自带的 `confirm/prompt`（部分浏览器会屏蔽），用 `window.AppDialog`。

## 7. 测试现状

- 运行：在项目根目录执行 `node --test "tests/*.test.cjs"`（Node 20+，不需要安装任何依赖）。当前 28 个测试全部通过，约 6 秒。
- `tests/helpers/app-harness.cjs`：在内存里的假 DOM + 可控音频时钟中运行原版 `metronome.js`，只在内存副本末尾注入 `window.__test`，不改发布代码。
- `tests/timing.test.cjs`：20–300 BPM × 1–8 细分全组合 1 分钟拍点、10 分钟无累计漂移、卡顿不补播、切细分/后台调速在下一拍生效、启动重入、渐进加速不污染保存值、预备拍调速。
- `tests/sync.test.cjs`：合并忽略坏数据、同时间戳两端结果一致、两台设备改不同练习速度都保留、旧版时间戳兼容、谱名只以文本写入列表、数字记谱往返。
- `tests/song-sync.test.cjs`：下载失败整轮取消不删谱、单份坏谱只跳过并用本机副本修复、换库不误删、播放中推迟同步。
- `tests/ui-errors.test.cjs`：扫码取消后迟到的授权/识别不再生效、重开扫码隔离旧会话、曲谱循环关闭、加载失败保留提示且阻止误播旧谱、重新选谱可恢复（摄像头和 alphaTab 事件均用模拟对象）。
- `tests/service-worker.test.cjs`：5xx 和断网回退缓存、导航入口回退、无缓存保留错误、正常响应及 404 不被旧缓存替换。
- 测试只能证明“排程时间算得对”，**证明不了手机喇叭实际出声准不准**。浏览器里的端到端检查（五个页面、同步、扫码）目前靠手动：同步用替换 `fetch` 的假 GitHub 接口，扫码用假的 `BarcodeDetector` + 画布视频流。
- 以下**从没在真机上验证过**：安卓上的实际声画延迟、后台/锁屏播放是否被系统掐断、横屏锁定、真实摄像头扫码、随机静音的听感。

### 真机验收清单（两台安卓都要做）
1. **节拍准**：60 / 120 / 200 BPM 各播 3 分钟，用另一台设备录音，在音频软件里量相邻拍间隔和总时长（3 分钟误差应在几毫秒内）；不应有漏拍、双击、忽快忽慢。
2. **调速**：播放中按 +5/−5、拖动滑块，听切换处是否在下一拍干净切换、没有杂音。
3. **后台/锁屏**：播放中切到别的 App、锁屏 1 分钟、5 分钟，看是否持续出声；回到前台后高亮是否马上对上。
4. **声画同步**：看高亮是否和声音同时，必要时调“声画同步校准”。
5. **随机静音、渐进加速、每遍预备拍**：听感是否符合预期。
6. **同步**：一台开启、一台扫码加入；两边各改一些东西，确认都能到对面。

## 8. 建议评审重点（下一轮）

1. **计时准确性**：已由第一轮评审修复并加测试（见上）；剩下的只能靠真机验收清单。
2. **云同步正确性**：`merge()` 的边界（时钟不准的设备、同时离线编辑、墓碑过期、旧版本设备缺字段）、两份 Gist 的一致性、限流（403/429）处理。
3. **数据安全**：导入的谱、`DRUM1:` 文本、Gist 内容都当作不可信输入——检查 `SyncBridge.apply`、`sanitizeCombo`、`parsePair` 的校验是否足够；有没有 XSS（谱名等用户文本是否都转义）。
4. **`metronome.js` 可维护性**：2400+ 行单文件，是否值得按模块拆分（音频引擎 / 各练习页 / 谱面绘制 / 设置）。
5. **移动端体验**：竖屏 375px、横屏 780×420、平板宽度下的布局；触控目标大小；双指缩放放开后是否有误触。
6. **性能与耗电**：8ms 的 `drain` 定时器、后台静音音频、曲谱页 alphaTab 重排。
7. **可补的自动化测试**：`layoutCombo()` 的分小节边界、`parsePair()` 的坏输入、扫码配对流程、Service Worker 的离线回退。

## 9. 已知取舍与遗留问题

- 调速、改细分要等到**下一拍**才生效（40 BPM 时最多约 1.5 秒），这是为了不打乱正在响的拍子。
- 后台被系统冻结超过 30 秒会直接停止播放并提示，而不是重新对齐接着播。
- 云端曲谱库里某一份内容损坏时只跳过那一份；本机有完好副本会自动上传修复，没有就提示“已损坏”。
- 曲谱页（alphaTab）的光标只能随声画校准**延后**，不能提前；它的节拍点击也不受“重音”开关控制。
- `metronome.js` 仍是 2400 行单文件，建议先补测试再按“音频引擎 / 练习页 / 谱面绘制 / 设置”拆分。

## 10. 近期提交

```
v48 第二轮修复：扫码取消与旧会话隔离、循环开关关闭、坏谱失败状态、服务器 5xx 回退缓存；新增 8 项回归测试
ac21002 第一轮评审修复：节拍排程重写为可撤销的检查点、过期不补播、启动防重入、同步逐项合并与坏数据校验、曲谱同步不误删、列表防注入、缓存只清本项目；新增 tests/
d0257c7 扫码配对；渐进加速、随机静音、每个练习记住速度；曲谱全屏 + 按小节循环；音量；默认无重音；声画延迟修正
69f181b Service Worker 改为网络优先 + 自动更新 + “更新到最新版”按钮
99d633c 横屏按钮、后台播放保活、变速练习每遍之间的预备拍
```
